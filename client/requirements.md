## Packages
framer-motion | Smooth animations for hero section and feature cards
lucide-react | Icons for features and UI elements (already in base, but ensuring)
zod | Schema validation (already in base)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  display: ["Space Grotesk", "sans-serif"],
}
API Endpoints:
- POST /api/subscribers (email signup)
- POST /api/messages (contact form)
